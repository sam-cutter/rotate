[package]
name = "rotate"
version = "0.1.0"
edition = "2024"

[dependencies]
good_lp = { version = "*", features = [
    "scip",
    "scip_bundled",
], default-features = false }
itertools = "0.14.0"
